<script>
    function readURLimg1(input) {
        if (input.files && input.files[0]) {
            var reader = new FileReader();

            reader.onload = function (e) {
                $('#img1')
                        .attr('src', e.target.result)
                //.width(300)
                //.height(200);
            };

            reader.readAsDataURL(input.files[0]);
        }
    }

    function readURLimg2(input) {
        if (input.files && input.files[0]) {
            var reader = new FileReader();

            reader.onload = function (e) {
                $('#img2')
                        .attr('src', e.target.result)
                //.width(300)
                //.height(200);
            };

            reader.readAsDataURL(input.files[0]);
        }
    }
</script>

<% style_session('avatar') %>
<%= render 'layerbegin' %>
<div class="ibox-content form-horizontal m-t">
  <h3>广告</h3>



  <%= form_for(@avater,:html => {:class=>'form-horizontal m-t',:multipart => true}) do |f| %>
      <br>
      <br>

      <div class="form-group">
        <label class="col-sm-2 control-label">目录广告(180*720)：</label>
        <div class="col-sm-2">
          <%= f.file_field :topavater ,class:"form-control" ,:onchange => "readURLimg1(this)" %>
              </div>
              <div>
                <img id="img1" width="200" src=<%= @avater.topavater.url %> >

              </div>
              </div>

              <div class="form-group">
                <label class="col-sm-2 control-label">考试广告(300*100)：</label>
                <div class="col-sm-2">
                  <%= f.file_field :leftavater ,class:"form-control" ,:onchange => "readURLimg2(this)"%>
                      </div>
                      <div>
                        <img id="img2" width="200" src=<%= @avater.leftavater.url %> >
                      </div>
                      </div>

                      <div class="form-group">
                        <label class="col-sm-2 control-label"></label>
                        <div class="col-sm-9">
                          <%= f.submit '确定',class: "btn btn-w-m btn-primary mylink pull-right",id:'smt','data-no-turbolink'=>true %>
                              </div>
                              </div>




                          <% end %>

                          </div>


                                      <%= render 'layerend' %>