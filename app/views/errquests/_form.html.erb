<%= form_for(@errquest) do |f| %>
  <% if @errquest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@errquest.errors.count, "error") %> prohibited this errquest from being saved:</h2>

      <ul>
      <% @errquest.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :amount %><br>
    <%= f.number_field :amount %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
